# Local tools (gh CLI, etc.) — not part of the project
.tools/

# Build artifacts
*.exe

# Downloaded archives
*.zip

# Spike test output
spike/out/

# Local build helper (env-specific MSVC path)
build.bat
