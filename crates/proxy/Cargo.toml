[package]
name = "claude-aegis-proxy"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Loopback CONNECT proxy that enforces a domain allow-list for an AppContainer sandbox — TLS passthrough (no MITM), with per-connection audit decisions."

[[bin]]
name = "claude-aegis-proxy"
path = "src/main.rs"

[dependencies]
serde_json = "1"
